pipeline {
    agent any

    stages {
        stage('hello world') {
            steps { 
                sh '
                 echo "without docker"'
                 
            }
        }
         
    }
}
